# FFmpeg mirror packages

Small FFmpeg runtime ZIP packages for the music-disc mod installer.

Each package contains only the `ffmpeg` executable for one platform/architecture so the files can be served directly from this repository without depending on the larger upstream archives at runtime.

See `manifest.json` for upstream source URLs, package sizes, and SHA-256 hashes.
